apiVersion: v1
kind: Service
metadata:
  name: devops-react-app-service
spec:
  type: NodePort
  selector:
    app: devops-react-app
  ports:
    - port: 5173
      targetPort: 5173
      nodePort: 30080